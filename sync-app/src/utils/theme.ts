import { createMuiTheme } from "@material-ui/core";

export const theme = createMuiTheme({
  palette: {
    primary: { main: "rgb(66, 96, 143)" },
    secondary: { main: "rgb(62, 137, 124)" },
    background: { paper: "transparent", default: "rgb(66, 96, 143)" },
  },
});
